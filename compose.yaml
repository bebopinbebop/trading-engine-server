name: firewatch

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8080:8080"

  frontend:
    build:
      context: ./frontend/firewatch
    ports:
      - "127.0.0.1:3000:3000"